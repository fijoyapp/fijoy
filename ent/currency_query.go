// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"beavermoney.app/ent/account"
	"beavermoney.app/ent/currency"
	"beavermoney.app/ent/household"
	"beavermoney.app/ent/investment"
	"beavermoney.app/ent/predicate"
	"beavermoney.app/ent/recurringsubscription"
	"beavermoney.app/ent/transactionentry"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CurrencyQuery is the builder for querying Currency entities.
type CurrencyQuery struct {
	config
	ctx                             *QueryContext
	order                           []currency.OrderOption
	inters                          []Interceptor
	predicates                      []predicate.Currency
	withAccounts                    *AccountQuery
	withInvestments                 *InvestmentQuery
	withTransactionEntries          *TransactionEntryQuery
	withHouseholds                  *HouseholdQuery
	withRecurringSubscriptions      *RecurringSubscriptionQuery
	loadTotal                       []func(context.Context, []*Currency) error
	modifiers                       []func(*sql.Selector)
	withNamedAccounts               map[string]*AccountQuery
	withNamedInvestments            map[string]*InvestmentQuery
	withNamedTransactionEntries     map[string]*TransactionEntryQuery
	withNamedHouseholds             map[string]*HouseholdQuery
	withNamedRecurringSubscriptions map[string]*RecurringSubscriptionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CurrencyQuery builder.
func (_q *CurrencyQuery) Where(ps ...predicate.Currency) *CurrencyQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CurrencyQuery) Limit(limit int) *CurrencyQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CurrencyQuery) Offset(offset int) *CurrencyQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CurrencyQuery) Unique(unique bool) *CurrencyQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CurrencyQuery) Order(o ...currency.OrderOption) *CurrencyQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAccounts chains the current query on the "accounts" edge.
func (_q *CurrencyQuery) QueryAccounts() *AccountQuery {
	query := (&AccountClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(account.Table, account.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, currency.AccountsTable, currency.AccountsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInvestments chains the current query on the "investments" edge.
func (_q *CurrencyQuery) QueryInvestments() *InvestmentQuery {
	query := (&InvestmentClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(investment.Table, investment.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, currency.InvestmentsTable, currency.InvestmentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTransactionEntries chains the current query on the "transaction_entries" edge.
func (_q *CurrencyQuery) QueryTransactionEntries() *TransactionEntryQuery {
	query := (&TransactionEntryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(transactionentry.Table, transactionentry.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, currency.TransactionEntriesTable, currency.TransactionEntriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryHouseholds chains the current query on the "households" edge.
func (_q *CurrencyQuery) QueryHouseholds() *HouseholdQuery {
	query := (&HouseholdClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(household.Table, household.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, currency.HouseholdsTable, currency.HouseholdsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRecurringSubscriptions chains the current query on the "recurring_subscriptions" edge.
func (_q *CurrencyQuery) QueryRecurringSubscriptions() *RecurringSubscriptionQuery {
	query := (&RecurringSubscriptionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(currency.Table, currency.FieldID, selector),
			sqlgraph.To(recurringsubscription.Table, recurringsubscription.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, currency.RecurringSubscriptionsTable, currency.RecurringSubscriptionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Currency entity from the query.
// Returns a *NotFoundError when no Currency was found.
func (_q *CurrencyQuery) First(ctx context.Context) (*Currency, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{currency.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CurrencyQuery) FirstX(ctx context.Context) *Currency {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Currency ID from the query.
// Returns a *NotFoundError when no Currency ID was found.
func (_q *CurrencyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{currency.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CurrencyQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Currency entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Currency entity is found.
// Returns a *NotFoundError when no Currency entities are found.
func (_q *CurrencyQuery) Only(ctx context.Context) (*Currency, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{currency.Label}
	default:
		return nil, &NotSingularError{currency.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CurrencyQuery) OnlyX(ctx context.Context) *Currency {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Currency ID in the query.
// Returns a *NotSingularError when more than one Currency ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CurrencyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{currency.Label}
	default:
		err = &NotSingularError{currency.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CurrencyQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Currencies.
func (_q *CurrencyQuery) All(ctx context.Context) ([]*Currency, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Currency, *CurrencyQuery]()
	return withInterceptors[[]*Currency](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CurrencyQuery) AllX(ctx context.Context) []*Currency {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Currency IDs.
func (_q *CurrencyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(currency.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CurrencyQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CurrencyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CurrencyQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CurrencyQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CurrencyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CurrencyQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CurrencyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CurrencyQuery) Clone() *CurrencyQuery {
	if _q == nil {
		return nil
	}
	return &CurrencyQuery{
		config:                     _q.config,
		ctx:                        _q.ctx.Clone(),
		order:                      append([]currency.OrderOption{}, _q.order...),
		inters:                     append([]Interceptor{}, _q.inters...),
		predicates:                 append([]predicate.Currency{}, _q.predicates...),
		withAccounts:               _q.withAccounts.Clone(),
		withInvestments:            _q.withInvestments.Clone(),
		withTransactionEntries:     _q.withTransactionEntries.Clone(),
		withHouseholds:             _q.withHouseholds.Clone(),
		withRecurringSubscriptions: _q.withRecurringSubscriptions.Clone(),
		// clone intermediate query.
		sql:       _q.sql.Clone(),
		path:      _q.path,
		modifiers: append([]func(*sql.Selector){}, _q.modifiers...),
	}
}

// WithAccounts tells the query-builder to eager-load the nodes that are connected to
// the "accounts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithAccounts(opts ...func(*AccountQuery)) *CurrencyQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAccounts = query
	return _q
}

// WithInvestments tells the query-builder to eager-load the nodes that are connected to
// the "investments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithInvestments(opts ...func(*InvestmentQuery)) *CurrencyQuery {
	query := (&InvestmentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInvestments = query
	return _q
}

// WithTransactionEntries tells the query-builder to eager-load the nodes that are connected to
// the "transaction_entries" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithTransactionEntries(opts ...func(*TransactionEntryQuery)) *CurrencyQuery {
	query := (&TransactionEntryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTransactionEntries = query
	return _q
}

// WithHouseholds tells the query-builder to eager-load the nodes that are connected to
// the "households" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithHouseholds(opts ...func(*HouseholdQuery)) *CurrencyQuery {
	query := (&HouseholdClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHouseholds = query
	return _q
}

// WithRecurringSubscriptions tells the query-builder to eager-load the nodes that are connected to
// the "recurring_subscriptions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithRecurringSubscriptions(opts ...func(*RecurringSubscriptionQuery)) *CurrencyQuery {
	query := (&RecurringSubscriptionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRecurringSubscriptions = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Currency.Query().
//		GroupBy(currency.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CurrencyQuery) GroupBy(field string, fields ...string) *CurrencyGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CurrencyGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = currency.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.Currency.Query().
//		Select(currency.FieldCode).
//		Scan(ctx, &v)
func (_q *CurrencyQuery) Select(fields ...string) *CurrencySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CurrencySelect{CurrencyQuery: _q}
	sbuild.label = currency.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CurrencySelect configured with the given aggregations.
func (_q *CurrencyQuery) Aggregate(fns ...AggregateFunc) *CurrencySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CurrencyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !currency.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CurrencyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Currency, error) {
	var (
		nodes       = []*Currency{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withAccounts != nil,
			_q.withInvestments != nil,
			_q.withTransactionEntries != nil,
			_q.withHouseholds != nil,
			_q.withRecurringSubscriptions != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Currency).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Currency{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAccounts; query != nil {
		if err := _q.loadAccounts(ctx, query, nodes,
			func(n *Currency) { n.Edges.Accounts = []*Account{} },
			func(n *Currency, e *Account) { n.Edges.Accounts = append(n.Edges.Accounts, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInvestments; query != nil {
		if err := _q.loadInvestments(ctx, query, nodes,
			func(n *Currency) { n.Edges.Investments = []*Investment{} },
			func(n *Currency, e *Investment) { n.Edges.Investments = append(n.Edges.Investments, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTransactionEntries; query != nil {
		if err := _q.loadTransactionEntries(ctx, query, nodes,
			func(n *Currency) { n.Edges.TransactionEntries = []*TransactionEntry{} },
			func(n *Currency, e *TransactionEntry) {
				n.Edges.TransactionEntries = append(n.Edges.TransactionEntries, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withHouseholds; query != nil {
		if err := _q.loadHouseholds(ctx, query, nodes,
			func(n *Currency) { n.Edges.Households = []*Household{} },
			func(n *Currency, e *Household) { n.Edges.Households = append(n.Edges.Households, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRecurringSubscriptions; query != nil {
		if err := _q.loadRecurringSubscriptions(ctx, query, nodes,
			func(n *Currency) { n.Edges.RecurringSubscriptions = []*RecurringSubscription{} },
			func(n *Currency, e *RecurringSubscription) {
				n.Edges.RecurringSubscriptions = append(n.Edges.RecurringSubscriptions, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedAccounts {
		if err := _q.loadAccounts(ctx, query, nodes,
			func(n *Currency) { n.appendNamedAccounts(name) },
			func(n *Currency, e *Account) { n.appendNamedAccounts(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedInvestments {
		if err := _q.loadInvestments(ctx, query, nodes,
			func(n *Currency) { n.appendNamedInvestments(name) },
			func(n *Currency, e *Investment) { n.appendNamedInvestments(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedTransactionEntries {
		if err := _q.loadTransactionEntries(ctx, query, nodes,
			func(n *Currency) { n.appendNamedTransactionEntries(name) },
			func(n *Currency, e *TransactionEntry) { n.appendNamedTransactionEntries(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedHouseholds {
		if err := _q.loadHouseholds(ctx, query, nodes,
			func(n *Currency) { n.appendNamedHouseholds(name) },
			func(n *Currency, e *Household) { n.appendNamedHouseholds(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedRecurringSubscriptions {
		if err := _q.loadRecurringSubscriptions(ctx, query, nodes,
			func(n *Currency) { n.appendNamedRecurringSubscriptions(name) },
			func(n *Currency, e *RecurringSubscription) { n.appendNamedRecurringSubscriptions(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CurrencyQuery) loadAccounts(ctx context.Context, query *AccountQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *Account)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(account.FieldCurrencyID)
	}
	query.Where(predicate.Account(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.AccountsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrencyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currency_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CurrencyQuery) loadInvestments(ctx context.Context, query *InvestmentQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *Investment)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(investment.FieldCurrencyID)
	}
	query.Where(predicate.Investment(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.InvestmentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrencyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currency_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CurrencyQuery) loadTransactionEntries(ctx context.Context, query *TransactionEntryQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *TransactionEntry)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(transactionentry.FieldCurrencyID)
	}
	query.Where(predicate.TransactionEntry(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.TransactionEntriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrencyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currency_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CurrencyQuery) loadHouseholds(ctx context.Context, query *HouseholdQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *Household)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(household.FieldCurrencyID)
	}
	query.Where(predicate.Household(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.HouseholdsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrencyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currency_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CurrencyQuery) loadRecurringSubscriptions(ctx context.Context, query *RecurringSubscriptionQuery, nodes []*Currency, init func(*Currency), assign func(*Currency, *RecurringSubscription)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Currency)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(recurringsubscription.FieldCurrencyID)
	}
	query.Where(predicate.RecurringSubscription(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(currency.RecurringSubscriptionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CurrencyID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "currency_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CurrencyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CurrencyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(currency.Table, currency.Columns, sqlgraph.NewFieldSpec(currency.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, currency.FieldID)
		for i := range fields {
			if fields[i] != currency.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CurrencyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(currency.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = currency.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_q *CurrencyQuery) Modify(modifiers ...func(s *sql.Selector)) *CurrencySelect {
	_q.modifiers = append(_q.modifiers, modifiers...)
	return _q.Select()
}

// WithNamedAccounts tells the query-builder to eager-load the nodes that are connected to the "accounts"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithNamedAccounts(name string, opts ...func(*AccountQuery)) *CurrencyQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedAccounts == nil {
		_q.withNamedAccounts = make(map[string]*AccountQuery)
	}
	_q.withNamedAccounts[name] = query
	return _q
}

// WithNamedInvestments tells the query-builder to eager-load the nodes that are connected to the "investments"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithNamedInvestments(name string, opts ...func(*InvestmentQuery)) *CurrencyQuery {
	query := (&InvestmentClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedInvestments == nil {
		_q.withNamedInvestments = make(map[string]*InvestmentQuery)
	}
	_q.withNamedInvestments[name] = query
	return _q
}

// WithNamedTransactionEntries tells the query-builder to eager-load the nodes that are connected to the "transaction_entries"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithNamedTransactionEntries(name string, opts ...func(*TransactionEntryQuery)) *CurrencyQuery {
	query := (&TransactionEntryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedTransactionEntries == nil {
		_q.withNamedTransactionEntries = make(map[string]*TransactionEntryQuery)
	}
	_q.withNamedTransactionEntries[name] = query
	return _q
}

// WithNamedHouseholds tells the query-builder to eager-load the nodes that are connected to the "households"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithNamedHouseholds(name string, opts ...func(*HouseholdQuery)) *CurrencyQuery {
	query := (&HouseholdClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedHouseholds == nil {
		_q.withNamedHouseholds = make(map[string]*HouseholdQuery)
	}
	_q.withNamedHouseholds[name] = query
	return _q
}

// WithNamedRecurringSubscriptions tells the query-builder to eager-load the nodes that are connected to the "recurring_subscriptions"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CurrencyQuery) WithNamedRecurringSubscriptions(name string, opts ...func(*RecurringSubscriptionQuery)) *CurrencyQuery {
	query := (&RecurringSubscriptionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedRecurringSubscriptions == nil {
		_q.withNamedRecurringSubscriptions = make(map[string]*RecurringSubscriptionQuery)
	}
	_q.withNamedRecurringSubscriptions[name] = query
	return _q
}

// CurrencyGroupBy is the group-by builder for Currency entities.
type CurrencyGroupBy struct {
	selector
	build *CurrencyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CurrencyGroupBy) Aggregate(fns ...AggregateFunc) *CurrencyGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CurrencyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyQuery, *CurrencyGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CurrencyGroupBy) sqlScan(ctx context.Context, root *CurrencyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CurrencySelect is the builder for selecting fields of Currency entities.
type CurrencySelect struct {
	*CurrencyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CurrencySelect) Aggregate(fns ...AggregateFunc) *CurrencySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CurrencySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CurrencyQuery, *CurrencySelect](ctx, _s.CurrencyQuery, _s, _s.inters, v)
}

func (_s *CurrencySelect) sqlScan(ctx context.Context, root *CurrencyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (_s *CurrencySelect) Modify(modifiers ...func(s *sql.Selector)) *CurrencySelect {
	_s.modifiers = append(_s.modifiers, modifiers...)
	return _s
}
