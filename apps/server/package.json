{
  "name": "@fijoy/server",
  "version": "0.0.0",
  "scripts": {
    "dev": "air",
    "build": "go build -tags netgo -ldflags '-s -w' -o ./dist/app ./cmd/fijoy/main.go",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "golangci-lint run"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-sql": "^0.18.1"
  },
  "packageManager": "pnpm@9.15.2"
}
